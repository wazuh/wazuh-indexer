---
- name: network
  title: Network
  type: group
  group: 2
  description: >
    Network related data.
  fields:
    - name: dhcp
      type: keyword
      level: custom
      description: >
        DHCP status (enabled, disabled, unknown, BOOTP)
    - name: gateway
      type: ip
      level: custom
      description: >
        Gateway address
    - name: metric
      type: long
      level: custom
      description: >
        Metric of the network protocol
