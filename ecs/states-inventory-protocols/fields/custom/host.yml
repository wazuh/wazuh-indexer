---
- name: host
  reusable:
    top_level: true
    expected:
      - { at: agent, as: host }
  fields:
    - name: ip
      description: >
        IP address
      type: ip
      level: custom
    - name: architecture
      description: >
        Architecture type
      type: keyword
      level: custom