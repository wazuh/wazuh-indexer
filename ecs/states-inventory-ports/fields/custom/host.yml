---
- name: host
  reusable:
    top_level: true
    expected:
      - { at: agent, as: host }
  fields:
    - name: ip
      description: >
        IP address
      type: ip
      level: custom
    - name: architecture
      description: >
        Architecture type
      type: keyword
      level: custom
    - name: network.ingress.queue
      type: long
      level: custom
      description: >
        Receive queue length.
    - name: network.egress.queue
      type: long
      level: custom
      description: >
        Transmit queue length.---
