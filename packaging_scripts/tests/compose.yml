services:

  wazuh-indexer-upgrade-config-merge-tests:
    build:
      context: ../../
      dockerfile: packaging_scripts/tests/Dockerfile
    working_dir: /workspace/packaging_scripts/tests
    volumes:
      # Mount repo root at /workspace
      - ../..:/workspace:rw

    # command will be provided by `docker compose run`