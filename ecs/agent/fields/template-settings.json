{
  "index_patterns": [
    ".agents"
  ],
  "priority": 1,
  "template": {
    "settings": {
      "index": {
        "codec": "zstd",
        "number_of_shards": "1",
        "number_of_replicas": "0",
        "refresh_interval": "5s",
        "query.default_field": [
          "agent.id",
          "agent.name",
          "agent.type",
          "agent.version",
          "agent.name"
        ]
      }
    }
  }
}